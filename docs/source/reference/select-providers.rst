.. Generated by scripts/generate_components.py; do not edit.

Documented components
=====================

Current development implementation. Metadata and examples below come from
the component catalogue; they are not a promise of full legacy compatibility.

checkBoxText
------------

Select multiple codes through inline or popup checkboxes.

Collection: ``inputs``. Tag: ``gnr-checkboxtext``.

Server required: no.

Child tags: none.

Parameters
~~~~~~~~~~

* ``value`` — str | None; optional; default: ``null``.

  Bound comma-separated codes; no selection is null.

* ``values`` — str | Bag; optional; default: ``null``.

  Code:label string or Bag of options; reactive bindings supported.

* ``identifier`` — str | None; optional; default: ``null``.

  Bag row field/attribute containing the code; defaults to node label.

* ``labelAttribute`` — str | None; optional; default: ``null``.

  Bag row field/attribute containing the caption; defaults to node label.

* ``popup`` — bool; optional; default: ``false``.

  False for inline checkboxes; true for a readonly display field with popup.

* ``cols`` — int; optional; default: ``1``.

  Number of checkbox columns; defaults to 1.

* ``disabled`` — bool; optional; default: ``false``.

  Disable user interaction.

* ``readonly`` — bool; optional; default: ``false``.

  Prevent user changes while preserving the displayed selection.

Provider request
~~~~~~~~~~~~~~~~

Provider response
~~~~~~~~~~~~~~~~~

Behavior and limits
~~~~~~~~~~~~~~~~~~~

* The bound Data node carries _displayedValue as comma-separated labels, or null.

* Unknown selected codes are retained and displayed literally; option updates never silently discard selection.

* No text filtering, remote/table options, hierarchical mode or valuesCb in this version.

Multiple dataset selection
~~~~~~~~~~~~~~~~~~~~~~~~~~

.. code-block:: python

   pane.checkBoxText(value='^.datasetFields', values='cost:Cost,revenue:Revenue', popup=True, cols=1)

dbSelect
--------

Select a database identity through a configured minimum service or an explicit endpoint.

Collection: ``inputs``. Tag: ``gnr-dbselect``.

Server required: yes.

Child tags: none.

Parameters
~~~~~~~~~~

* ``dbtable`` — string; optional; default: ``null``.

  Configured logical table for the standard service. Requires a page with DbPageMixin and a configured DbHandler service.

* ``ignoreCase`` — boolean | Data binding; optional; default: ``true``.

  Standard service matching policy. SQLite uses Unicode casefold when true and exact case when false.

* ``limit`` — integer | Data binding; optional; default: ``10``.

  Standard service result limit, from 1 to 100. Prefix matching runs first; containment is tried only when no prefix matches.

* ``rpcmethod`` — @endpoint method | logical method name; optional; default: ``null``.

  Explicit endpoint for existing providers. When omitted, Python authoring requires dbtable and uses dbhandler.dbselect.

* ``value`` — identity | Data binding; optional; default: ``null``.

  Selected identity, not caption. Use ^path for two-way Data binding. Identity is normalized to a string in the browser; null clears the selection.

* ``kw_*`` — any | Data binding; optional; default: ``null``.

  Provider parameters: the prefix is removed and bindings are resolved on each request. Use =path for dependent scope. Clear dependent selections explicitly when that scope changes.

* ``searchdelay`` — number; optional; default: ``300``.

  Debounce delay in milliseconds before text search.

* ``selectedCaption`` — Data path; optional; default: ``null``.

  Destination receiving the caption when the user chooses a row.

* ``selected_*`` — Data path; optional; default: ``null``.

  Destination receiving the selected record field named after the prefix. A missing field writes null. These outputs are not automatically cleared when value is cleared.

* ``lbl`` — string; optional; default: ``null``.

  Field label through the shared control decoration.

* ``placeholder`` — string; optional; default: ``null``.

  Hint shown by the empty input.

* ``disabled`` — boolean; optional; default: ``false``.

  Disable user interaction.

* ``readOnly`` — boolean; optional; default: ``false``.

  Prevent user editing.

* ``width`` — CSS length; optional; default: ``null``.

  Width through standard Gramlot styling.

Provider request
~~~~~~~~~~~~~~~~

* ``_querystring`` — string; optional; default: ``""``.

  Search text supplied by the widget. The provider owns matching and result limits.

* ``_id`` — identity; optional; default: ``null``.

  Exact identity lookup instead of text search, used to restore the caption of a stored value.

Provider response
~~~~~~~~~~~~~~~~~

* ``rows`` — array of records; required; default: ``null``.

  Rows with unique nonempty identities and non-null captions.

* ``identifier`` — string; required; default: ``null``.

  Name of the identity field in every row.

* ``caption`` — string; required; default: ``null``.

  Name of the display-caption field in every row.

* ``metadata`` — object; optional; default: ``{}``.

  Optional metadata retained on the component as resultMetadata.

Behavior and limits
~~~~~~~~~~~~~~~~~~~

* Uses filteringSelect keyboard and constrained identity selection behavior. Standard Gramlot bindings, field state and layout attributes remain available; this parameter list documents the provider contract and common controls.

* At most one provider request is in flight per SourceNode. Another request is refused with busy feedback, without queue or replay. Obsolete results are discarded. Clearing value invalidates cached choices and pending results.

* Providers must implement both text search and exact identity lookup. No SQL/table inference, automatic paging, legacy headers/data response, or multi-column popup is provided.

* These metadata describe the current runtime; they do not introduce strict parameter type validation.

* The example requires the optional FastAPI–GenroPy host and test_invoice_pg model. The component itself is database-agnostic; the endpoint owns database access and authorization.

* The new Python dbtable path is an experimental minimum service. Its SQLite adapter is read-only and searches the configured caption column. Advanced options such as auxColumns are rejected; Genropy-specific capabilities remain separate. Existing explicit-rpcmethod providers retain their own contracts.

Minimal dbSelect
~~~~~~~~~~~~~~~~

.. code-block:: python

   from gramlot_fastapi.genropy import GenropyPage
   from gramlot.page import endpoint


   class Page(GenropyPage):
       def main(self, root):
           root.dbSelect(value='^customer_id', rpcmethod=self.customers, lbl='Customer')

       @endpoint
       def customers(self, _querystring='', _id=None):
           where = '$id=:key' if _id is not None else '$account_name ILIKE :text'
           rows = self.db.table('invc.customer').query(
               columns='$id,$account_name', where=where, key=_id,
               text=f'%{_querystring}%', order_by='$account_name,$id', limit=10).fetch()
           return dict(self.selection_result(rows, identifier='id'), caption='account_name')

remoteSelect
------------

Select an identity from an arbitrary server endpoint.

Collection: ``inputs``. Tag: ``gnr-remoteselect``.

Server required: yes.

Child tags: none.

Parameters
~~~~~~~~~~

* ``rpcmethod`` — @endpoint method | logical method name; required; default: ``null``.

  Explicitly exposed data endpoint. Python accepts the decorated bound method or its logical name.

* ``value`` — identity | Data binding; optional; default: ``null``.

  Selected identity, not caption. Use ^path for two-way Data binding. Identity is normalized to a string in the browser; null clears the selection.

* ``kw_*`` — any | Data binding; optional; default: ``null``.

  Provider parameters: the prefix is removed and bindings are resolved on each request. Use =path for dependent scope. Clear dependent selections explicitly when that scope changes.

* ``searchdelay`` — number; optional; default: ``300``.

  Debounce delay in milliseconds before text search.

* ``selectedCaption`` — Data path; optional; default: ``null``.

  Destination receiving the caption when the user chooses a row.

* ``selected_*`` — Data path; optional; default: ``null``.

  Destination receiving the selected record field named after the prefix. A missing field writes null. These outputs are not automatically cleared when value is cleared.

* ``lbl`` — string; optional; default: ``null``.

  Field label through the shared control decoration.

* ``placeholder`` — string; optional; default: ``null``.

  Hint shown by the empty input.

* ``disabled`` — boolean; optional; default: ``false``.

  Disable user interaction.

* ``readOnly`` — boolean; optional; default: ``false``.

  Prevent user editing.

* ``width`` — CSS length; optional; default: ``null``.

  Width through standard Gramlot styling.

Provider request
~~~~~~~~~~~~~~~~

* ``_querystring`` — string; optional; default: ``""``.

  Search text supplied by the widget. The provider owns matching and result limits.

* ``_id`` — identity; optional; default: ``null``.

  Exact identity lookup instead of text search, used to restore the caption of a stored value.

Provider response
~~~~~~~~~~~~~~~~~

* ``rows`` — array of records; required; default: ``null``.

  Rows with unique nonempty identities and non-null captions.

* ``identifier`` — string; required; default: ``null``.

  Name of the identity field in every row.

* ``caption`` — string; required; default: ``null``.

  Name of the display-caption field in every row.

* ``metadata`` — object; optional; default: ``{}``.

  Optional metadata retained on the component as resultMetadata.

Behavior and limits
~~~~~~~~~~~~~~~~~~~

* Uses filteringSelect keyboard and constrained identity selection behavior. Standard Gramlot bindings, field state and layout attributes remain available; this parameter list documents the provider contract and common controls.

* At most one provider request is in flight per SourceNode. Another request is refused with busy feedback, without queue or replay. Obsolete results are discarded. Clearing value invalidates cached choices and pending results.

* Providers must implement both text search and exact identity lookup. No SQL/table inference, automatic paging, legacy headers/data response, or multi-column popup is provided.

* These metadata describe the current runtime; they do not introduce strict parameter type validation.

Minimal remoteSelect
~~~~~~~~~~~~~~~~~~~~

.. code-block:: python

   from gramlot.page import WebPage, endpoint


   class Page(WebPage):
       def main(self, root):
           root.remoteSelect(value='^choice', rpcmethod=self.choices, lbl='Choice')

       @endpoint
       def choices(self, _querystring='', _id=None):
           rows = [dict(id='a', name='Alpha'), dict(id='b', name='Beta')]
           rows = [r for r in rows if (r['id'] == _id if _id is not None
                                      else _querystring.lower() in r['name'].lower())]
           return dict(rows=rows, identifier='id', caption='name')

callbackSelect
--------------

Select an identity through a browser callback, synchronous or asynchronous.

Collection: ``inputs``. Tag: ``gnr-callbackselect``.

Server required: no.

Child tags: none.

Parameters
~~~~~~~~~~

* ``callback`` — JavaScript body | function; required; default: ``null``.

  Browser code receiving kw and this=SourceNode; return the selection object or a Promise of it.

* ``value`` — identity | Data binding; optional; default: ``null``.

  Selected identity, not caption. Use ^path for two-way Data binding. Identity is normalized to a string in the browser; null clears the selection.

* ``kw_*`` — any | Data binding; optional; default: ``null``.

  Provider parameters: the prefix is removed and bindings are resolved on each request. Use =path for dependent scope. Clear dependent selections explicitly when that scope changes.

* ``searchdelay`` — number; optional; default: ``300``.

  Debounce delay in milliseconds before text search.

* ``selectedCaption`` — Data path; optional; default: ``null``.

  Destination receiving the caption when the user chooses a row.

* ``selected_*`` — Data path; optional; default: ``null``.

  Destination receiving the selected record field named after the prefix. A missing field writes null. These outputs are not automatically cleared when value is cleared.

* ``lbl`` — string; optional; default: ``null``.

  Field label through the shared control decoration.

* ``placeholder`` — string; optional; default: ``null``.

  Hint shown by the empty input.

* ``disabled`` — boolean; optional; default: ``false``.

  Disable user interaction.

* ``readOnly`` — boolean; optional; default: ``false``.

  Prevent user editing.

* ``width`` — CSS length; optional; default: ``null``.

  Width through standard Gramlot styling.

Provider request
~~~~~~~~~~~~~~~~

* ``_querystring`` — string; optional; default: ``""``.

  Search text supplied by the widget. The provider owns matching and result limits.

* ``_id`` — identity; optional; default: ``null``.

  Exact identity lookup instead of text search, used to restore the caption of a stored value.

Provider response
~~~~~~~~~~~~~~~~~

* ``rows`` — array of records; required; default: ``null``.

  Rows with unique nonempty identities and non-null captions.

* ``identifier`` — string; required; default: ``null``.

  Name of the identity field in every row.

* ``caption`` — string; required; default: ``null``.

  Name of the display-caption field in every row.

* ``metadata`` — object; optional; default: ``{}``.

  Optional metadata retained on the component as resultMetadata.

Behavior and limits
~~~~~~~~~~~~~~~~~~~

* Uses filteringSelect keyboard and constrained identity selection behavior. Standard Gramlot bindings, field state and layout attributes remain available; this parameter list documents the provider contract and common controls.

* At most one provider request is in flight per SourceNode. Another request is refused with busy feedback, without queue or replay. Obsolete results are discarded. Clearing value invalidates cached choices and pending results.

* Providers must implement both text search and exact identity lookup. No SQL/table inference, automatic paging, legacy headers/data response, or multi-column popup is provided.

* These metadata describe the current runtime; they do not introduce strict parameter type validation.

* Works without a server. Callback code is trusted application code, not user-supplied JavaScript.

Minimal callbackSelect
~~~~~~~~~~~~~~~~~~~~~~

.. code-block:: python

   from gramlot.page import WebPage


   class Page(WebPage):
       def main(self, root):
           root.callbackSelect(value='^choice', lbl='Choice', callback="""
               const rows = [{id: 'a', name: 'Alpha'}, {id: 'b', name: 'Beta'}];
               return {rows: rows.filter(r => kw._id != null ? r.id === kw._id
                   : r.name.toLowerCase().includes((kw._querystring || '').toLowerCase())),
                   identifier: 'id', caption: 'name'};
           """)

fileSystemTree
--------------

Browse a named server directory through lazy RPC directory resolvers.

Collection: ``storeTree``. Tag: ``gnr-filesystemtree``.

Server required: yes.

Child tags: none.

Parameters
~~~~~~~~~~

* ``root`` — string; required; default: ``null``.

  Name declared in filesystem_roots on the page; never an arbitrary server path.

* ``path`` — string; optional; default: ``""``.

  Relative subdirectory within that root.

* ``rpcmethod`` — endpoint name; optional; default: ``"directory_tree"``.

  Provider for the initial level. Nested resolver descriptors identify their own endpoint.

* ``storepath`` — Data path; optional; default: ``null``.

  Data destination; generated per declaration if omitted.

Provider request
~~~~~~~~~~~~~~~~

Provider response
~~~~~~~~~~~~~~~~~

Behavior and limits
~~~~~~~~~~~~~~~~~~~

* Read-only. Symbolic links are not exposed. Directory children are loaded on expansion. File contents and file mutations are not exposed.

gramlotIde
----------

Edit documents in Data Bags, with optional filesystem navigation and conflict-aware Save.

Collection: ``labEditors``. Tag: ``gnr-gramlotide``.

Server required: no.

Child tags: none.

Parameters
~~~~~~~~~~

* ``datapath`` — Data path; optional; default: ``null``.

  Document state scope. Python generates an isolated scope when omitted.

* ``root`` — string; optional; default: ``null``.

  Named server workspace. Omit for local document editing without RPC.

* ``initialpath`` — string; optional; default: ``null``.

  Workspace file opened initially.

* ``writable`` — boolean; optional; default: ``false``.

  Enable Save; the server must independently allow writing the workspace.

* ``treemethod`` — endpoint name; optional; default: ``"directory_tree"``.

  Directory provider.

* ``readmethod`` — endpoint name; optional; default: ``"document_read"``.

  Read provider returning content, revision, language and writable.

* ``savemethod`` — endpoint name; optional; default: ``"document_save"``.

  Save provider receiving root, path, content and revision.

* ``content`` — string; optional; default: ``null``.

  Initial standalone document content.

* ``filename`` — string; optional; default: ``"scratch.py"``.

  Name of the standalone initial document.

* ``language`` — string; optional; default: ``"python"``.

  Language of the standalone initial document.

Provider request
~~~~~~~~~~~~~~~~

Provider response
~~~~~~~~~~~~~~~~~

Behavior and limits
~~~~~~~~~~~~~~~~~~~

* Documents, active tab, saved baseline and revision live in Data Bags. Closing a dirty document requires explicit discard. Save errors retain edits.

* The default provider edits existing UTF-8 files only, limited to 2 MiB. No code execution, new files, rename, delete or rich-text HTML editor is implemented.

* HTML currently opens as source; visual HTML editing requires a separate rich-text component and is deferred.

* CodeMirror uses the existing pinned external modules and retains a textarea if they are unavailable.

Standalone scratch document
~~~~~~~~~~~~~~~~~~~~~~~~~~~

.. code-block:: python

   from gramlot.page import WebPage

   class Page(WebPage):
       def main(self, root):
           root.gramlotIde(content='print(42)', filename='scratch.py')
